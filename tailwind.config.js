module.exports = {
    purge: [],
    darkMode: false, // or 'media' or 'class'
    theme: {
        extend: {
            cursor: {
              default: 'url(/public/sprites/pointer_c.png), default',
            },
        },
    },
    variants: {
        extend: {},
    },
    plugins: [],
}